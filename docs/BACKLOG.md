# Backlog

**See [docs/changelog/2026-03-08-complexity-refactoring-queue.md](changelog/2026-03-08-complexity-refactoring-queue.md) for historical complexity refactoring metrics (434 → 0 offenders, 2026-03-08 refresh completed 2026-03-09).**






## Bugs

- [ ] **BUG-02** (P1) Deduplication executor silently no-ops when `replacements` keys don't match resolved plan paths — `_update_single_file` (`src/ast_grep_mcp/features/deduplication/applicator_executor.py:147`) looks up `replacements.get(file_path, {})` by the exact orchestration-plan path. `applicator._resolve_file_paths` (`applicator.py:397-404`) converts plan paths to **absolute** paths, while the pre-validator (`applicator_validator.py:86-90`, `_resolve_file_path`) explicitly accepts **relative** replacement keys by joining with `project_folder` — signaling relative keys are a supported input shape. When `generated_code.replacements` uses relative keys (e.g. `"src/foo.py"`), pre-validation passes, the executor's lookup with `/abs/project/src/foo.py` misses, and the file is read and rewritten **unchanged** (mtime touched, import possibly injected) — then reported in `files_modified` with `status: "success"`. The duplicate code is never actually replaced.
  - **Repro:** build an orchestration plan whose `update_files[].path` entries are project-relative, with `replacements` keyed the same way; run apply. Result: success response, zero content changes.
  - **Fix direction:** normalize both plan paths and replacement keys to absolute (or canonical relative) form in one place before execution — e.g. resolve replacement keys in `_resolve_file_paths` alongside `files_affected` — and/or treat a missing replacement entry for a planned update file as an error rather than an empty dict.
  - **Debugging context:** lookup miss returns `{}` so `replacement.get("new_content")` is falsy and the original content is written back at `applicator_executor.py:153-154`; related silent-skip at `applicator_executor.py:127-128` where planned files missing on disk are dropped without a `failed_files` entry. Verified 2026-07-21 (dedup module review).

- [x] **BUG-06** (P2) Per-candidate enrichment timeout is dead code; a hung enrichment blocks the tool forever — `analysis_orchestrator.py:481-484` calls `as_completed(futures)` **without** a timeout, then `future.result(timeout=timeout_seconds)` on futures that are already complete, which can never raise `TimeoutError`. The whole timeout apparatus (`_resolve_timeout`, the `TimeoutError` branch at `:401-407`, `DEFAULT_TIMEOUT_PER_CANDIDATE_SECONDS`) is unreachable; one hung coverage lookup blocks the MCP call indefinitely. Fix: `as_completed(futures, timeout=...)` or `concurrent.futures.wait` with deadlines. (2026-07-21 dedup module review)
  - **Fixed 2026-07-24:** `_process_parallel_enrichment` now iterates `futures.items()` so `future.result(timeout=)` enforces a live per-candidate timeout; executor shut down via `finally: shutdown(wait=False, cancel_futures=True)` so hung workers can't block return. Regression tests: `TestPerCandidateTimeoutEnforcement`. The follow-up xhigh review found 29 issues in and around the fix (see **BUG-06 Timeout Fix Code Review Findings** below); CR-01/CR-02/CR-25 fixed same day.
- [ ] **BUG-07** (P2) Exclude patterns applied after the stream limit truncates results — `detector.py:257-270` collects up to `max_constructs` (default 1000) matches from `stream_ast_grep_results` **before** `_apply_exclude_patterns` filters paths. If `.venv`/`node_modules` matches appear early in the walk they consume the entire budget and are then discarded, so real project code is never analyzed; the `construct_limit_reached` log compares the post-filter count, hiding the truncation. Fix: filter inside the stream loop (or pass excludes to ast-grep) so the limit applies to kept matches. (2026-07-21 dedup module review)
- [ ] **BUG-08** (P2) Extract-target file not covered by backup/rollback — `applicator.py:273-276` backs up only `files_to_modify`, but the plan (`applicator.py:535-544`) also writes/appends to `target_file`. On post-validation failure + rollback, an appended extracted function remains in an existing shared file (retry appends a duplicate definition), and newly created `_extracted_utils.py` files are never deleted (`applicator_backup.py:98` restores only files that existed at backup time). Fix: include `target_file` in the backup set and track created files for deletion on rollback. (2026-07-21 dedup module review)
- [ ] **BUG-09** (P2) Wrong dot count in generated parent-directory relative imports — `applicator.py:560-570`: for `target_rel="../shared/utils.py"`, `replace("/", ".")` yields `"...shared.utils"` and the `p != ".."` filter is dead code (`".."` never survives the split), then `generator.py:279-281` prepends another dot, producing `from ....shared.utils import f` where `from ..shared.utils import f` is correct. Post-validation is syntax-only, so this ships as "success" and fails at runtime with ImportError. (2026-07-21 dedup module review)
- [ ] **BUG-10** (P2) `_insert_python_import` matches indented function-local imports and demotes module docstrings — `applicator.py:605-626` uses `stripped.startswith("import "/"from ")`, so a lazy in-function import marks the insertion point mid-function at column 0 (syntax error → spurious rollback); in a file with a module docstring and no imports, the fallback inserts above the docstring, demoting it to a plain string expression. (2026-07-21 dedup module review)
- [ ] **BUG-11** (P2) Batch test-coverage check re-reads every test file per source file — `coverage.py:431-434` caches the *set* of test-file paths but `_check_test_file_references_source` re-reads each file's *content* on every call: O(n×m) file reads (500 test files × 100 candidates ≈ 50k reads), contradicting the docstring's claimed O(m+n). Fix: cache file contents (lowercased) in `_find_all_test_files`. (2026-07-21 dedup module review)
- [ ] **BUG-12** (P2) Similarity/embedding/score caches keyed by `hash()` with no collision check and no size bound — `similarity.py:219-221` (MinHash signatures), `similarity.py:1241-1248` (CodeBERT 768-float embeddings), `ranker.py:398` (`f"cache_{abs(hash(...))}"`). A 64-bit collision silently returns another snippet's data; caches grow unbounded in a long-lived MCP server process. Fix: key on the code string (or verify `(hash, code)`) and add an LRU bound. (2026-07-21 dedup module review)
- [ ] **BUG-13** (P2) Base snippet re-parsed via subprocess for every group member — `analyzer.py:151-176`, `:279-287`: `identify_varying_literals(base_code, code)` re-extracts `base_code`'s literals with 3 ast-grep subprocess spawns per comparison; for a group of N that's 6·(N−1) spawns where 3·(N−1)+3 suffice. Extract the base literals once and reuse. (2026-07-21 dedup module review)

## BUG-06 Timeout Fix Code Review Findings (2026-07-24)

**29 findings from the xhigh code review of the BUG-06 fix** (10 finder angles + per-finding adversarial verification + gap sweep; 29/29 survived — verdict CONFIRMED unless marked PLAUSIBLE). Review scope: the `_process_parallel_enrichment` rewrite in `analysis_orchestrator.py` (per-future `result(timeout=)`, `shutdown(wait=False, cancel_futures=True)`) plus the `TestPerCandidateTimeoutEnforcement` regression tests. Several findings were empirically reproduced on this repo's Python 3.13.13 (noted below). CR-24–29 came from the gap-sweep pass.

### Incomplete fix — BUG-06 hang persists on other paths

- [x] **BUG-06-CR-01** (P1) Default batch coverage path still hangs; bypasses the fix entirely — the live `analyze_deduplication_candidates` flow (`include_test_coverage=True` per `tools.py:74`/`config.py:76`) runs `_enrich_and_summarize_with_config` → `_add_test_coverage_batch` (`analysis_orchestrator.py:337`) → `get_test_coverage_for_files_batch` → `_process_parallel_batch`, which retains the exact pre-fix shape: `with ThreadPoolExecutor` (`coverage.py:557`, implicit `shutdown(wait=True)`), `as_completed(futures)` with no timeout (`coverage.py:565`), `future.result()` with no timeout (`coverage.py:612`).
  - **Repro:** one hung `_has_test_coverage_optimized` call (e.g. filesystem stall on a network mount) blocks the MCP call forever — the exact BUG-06 symptom, on the most I/O-bound path.
  - **Debugging context:** the fixed `_process_parallel_enrichment` is only reached via `_parallel_enrich`, whose callers are `_add_recommendations` and the legacy `_add_test_coverage` that no src code calls. `_add_test_coverage_batch` declares `timeout_per_candidate` (`analysis_orchestrator.py:601`) but never uses it.
  - **Fix direction:** port the corrected wait semantics into `coverage.py:_process_parallel_batch` (or route batch coverage through a shared helper — see CR-16), and thread `timeout_per_candidate` through.
  - **Fixed 2026-07-24:** `_process_parallel_batch` rewritten — bare executor + `finally: shutdown(wait=False, cancel_futures=True)`, iterates `futures.items()` with per-future `_get_future_result_with_timeout` (TimeoutError → log + `False`); `get_test_coverage_for_files_batch` gained `timeout_per_file` (defaults to `DEFAULT_TIMEOUT_PER_CANDIDATE_SECONDS`). Tests: `TestBatchCoverageParallelTimeout` in `test_batch_coverage.py`. Note: CR-16 (shared helper) still open — this is a second hand-rolled copy of the idiom.

- [x] **BUG-06-CR-02** (P2) Sequential/single-candidate path has no timeout at all — `_parallel_enrich` (`analysis_orchestrator.py:564`) routes `parallel=False` **or** `len(candidates) == 1` to `_process_sequential_enrichment`, which calls `enrich_func` inline with no timeout; `timeout_per_candidate` is silently ignored (docstring at `:557` claims "default 30s" with no parallel-only caveat). Live trigger: `config.parallel=False` or exactly one top candidate via `_add_recommendations` (`:343`). Both new regression tests use 2-3 candidates with `parallel=True`, so neither covers this branch. Fix: enforce the timeout invariant on both branches (single-worker pool for N=1, or a deadline wrapper), and add a single-candidate regression test.
  - **Fixed 2026-07-24:** `_process_sequential_enrichment` now takes `timeout_seconds` and runs each candidate through a fresh per-candidate `ThreadPoolExecutor(max_workers=1)` + `_process_completed_future`, so a hung candidate is abandoned after the timeout without blocking the next. Tests: `TestSequentialPathTimeoutEnforcement` (single-candidate and `parallel=False` cases). Note: inherits the parallel path's known caveats (CR-03 abandoned-worker mutation, CR-04 leaked threads, CR-08 worker-raised TimeoutError conflation).

- [ ] **BUG-06-CR-10** (P3, PLAUSIBLE) `ranker.py` parallel scoring uses the same untimed idiom — `_score_candidates_parallel` (`ranker.py:498-506`): `with ThreadPoolExecutor` + `as_completed` + `future.result()` no timeout, in the same analyze flow (Step 2, before enrichment). Scoring is pure in-memory arithmetic (no I/O; `_cache_lock` is a single non-nested lock), so no concrete hang trigger was named — but it is the same defect class in the same module. Fix alongside CR-16 extraction.

### Correctness of the new code

- [x] **BUG-06-CR-03** (P2) Abandoned worker mutates candidate dict after timeout — `analysis_orchestrator.py:481` submits the *shared* candidate dict; on timeout the main thread stamps error fields and returns, but `shutdown(wait=False, cancel_futures=True)` (`:492`) only cancels never-started futures. **Reproduced:** candidate at return `{'test_coverage_error': 'Operation timed out after 1s', 'has_tests': False}` mutated after the leaked thread finished to `{'test_coverage': {...}, 'has_tests': True}` — a contradictory payload written into the exact dicts `_build_enrich_result` returns (`"candidates": top_candidates`), racing downstream stages and MCP JSON serialization (worst case `RuntimeError: dictionary changed size during iteration`). Fix: workers write to a private copy/result holder; merge onto the candidate only when `result()` succeeds.
  - **Fixed 2026-07-25:** both enrichment paths submit `copy.deepcopy(candidate)`; the copy merges onto the original only on success (`candidate.update(working)`), so an abandoned worker mutates only the orphaned copy, and an enrich_func that mutates then raises leaks no partial writes. Accepted trade-offs documented: key deletions don't propagate; nested-value identities replaced on merge. Uncopyable candidates fail alone (routed through the per-candidate error path) rather than aborting the stage. Tests: `TestAbandonedWorkerIsolation` (5 tests). **Adversarially verified** (3 lenses): stress probe 30 rounds × 24 candidates × 8 workers found zero leaked/torn merges; happens-before edge via the future's condition lock confirmed; both production enrich funcs safe with shared kwargs. Side effect: the harmful half of CR-06 (late-run overwriting error state) is neutralized — its wasted-work/mislabeling half stays open.

- [ ] **BUG-06-CR-04** (P2) Leaked hung non-daemon threads block interpreter exit and accumulate — verified on Python 3.13.13: pool workers are non-daemon and registered in `_threads_queues`; the atexit-registered `_python_exit` joins every worker, so one permanently hung enrichment makes clean exit (stdio EOF, `sys.exit`, handled SIGTERM — the normal MCP stdio shutdown path) hang until SIGKILL, relocating BUG-06 from tool-call time to process-exit time. Each hung call leaks up to `max_workers` (default 4) threads pinning candidate refs; nothing counts or logs abandoned workers. Fix: log/count abandoned workers per call; longer-term enforce the timeout at the blocking primitive (subprocess/file timeouts inside coverage detection) or use a process pool for hang-prone enrichment.

- [ ] **BUG-06-CR-05** (P2) No global deadline: worst case N × timeout — the loop (`analysis_orchestrator.py:486`) waits futures serially in submission order, each with a fresh full `timeout_seconds`; 100 candidates (`DeduplicationDefaults.MAX_CANDIDATES`, `constants.py:196`) × 30s default ≈ 50 minutes when all hang — operationally indistinguishable from the original hang for an MCP client. `ParallelProcessing.MAX_TIMEOUT_SECONDS=300` ("5 minutes max total timeout", `constants.py:43`) is enforced nowhere (only referenced by a test asserting it exists). Fix: shared deadline — `future.result(timeout=max(0, deadline - time.monotonic()))` or `concurrent.futures.wait(futures, timeout=T)` then mark `not_done` timed out; total wall-clock ~T instead of N×T (submission-order iteration is safe since finished results are cached).

- [ ] **BUG-06-CR-06** (P2→P3) Timed-out pending future never cancelled; may still run later — the `except TimeoutError` branch (`analysis_orchestrator.py:440-445`) doesn't call `future.cancel()`, and `cancel_futures=True` only fires in the `finally` after the whole loop. Trigger: `max_workers=1`, A hangs 65s, timeout 30s — B times out while queued at t=60, is marked failed with default error values, then at t=65 A's worker frees and *runs B anyway*, overwriting the error state while B sits in `failed_candidates`. Fix: `future.cancel()` inside the TimeoutError handler (succeeds on pending, harmless no-op on running).
  - **Downgraded 2026-07-25:** the CR-03 fix neutralizes the harmful half — a late-run future now enriches only its orphaned copy (the merge requires `result()` success, which a timed-out wait already forfeited), so error state can no longer be overwritten. Remaining cost: wasted work and the never-started/timed-out mislabeling (CR-07). The `future.cancel()` one-liner now belongs in the shared helper `utils/futures.py` (see CR-16 fix).

- [ ] **BUG-06-CR-07** (P3) Never-started candidates mislabeled as timed out — **reproduced:** `max_workers=2`, 3 hung candidates, T=1s: `enrich_func` ran only for c0/c1, yet c2 (never dequeued) was stamped `error="Operation timed out after 1s"` with an identical `{operation}_timeout` log entry, after burning its own full 1s serial wait. Telemetry shows N slow enrichments when some were merely starved. `future.cancel()` returning True distinguishes never-started from ran-and-timed-out; a shared deadline (CR-05) marks expired pending futures in O(1). Note: the second regression test asserts the mislabeled text, baking the behavior in.

- [ ] **BUG-06-CR-08** (P3) Worker-raised `TimeoutError` misclassified as wait timeout — verified on 3.13: `concurrent.futures.TimeoutError`, `socket.timeout`, `asyncio.TimeoutError` are all the builtin `TimeoutError`, and `OSError(ETIMEDOUT)` auto-maps to it. **Reproduced:** an enrich_func failing instantly with `OSError(ETIMEDOUT, 'read ... timed out after 2s')` is re-raised by `future.result(timeout=30)` and recorded as `'Operation timed out after 30s'` — real error text discarded, nonexistent 30s hang fabricated. Fix: disambiguate with `future.done()` in the handler (True after worker exception; False after a genuine wait timeout).

- [ ] **BUG-06-CR-09** (P3) Sequential-path `TimeoutError` yields `'Operation timed out after Nones'` — **reproduced:** `_process_sequential_enrichment` (`:524`) calls `_handle_enrichment_error` without `error_message`; a builtin TimeoutError from enrich_func hits the isinstance branch (`:401`) and formats `f"...after {None}s"`, logging `timeout_seconds=None`. Even on the parallel path the structured `timeout_seconds` log field carries the string `'30'`, not a number. Pre-existing, but adjacent to the fix. Fix with CR-14's refactor.

- [ ] **BUG-06-CR-11** (P4, PLAUSIBLE) `timeout_per_candidate=0` now instantly fails running futures — `_resolve_timeout` (`analysis_orchestrator.py:531-532`) passes 0 through (`is not None` guard), and `future.result(timeout=0)` raises TimeoutError immediately for any unfinished future (verified). Previously harmless dead code. No current call site passes 0 (param is private, unexposed via MCP), so latent. Fix: one-line guard — treat `<=0` as None or reject it.

### Refactoring support (src)

- [x] **BUG-06-CR-12** (P3) `_process_completed_future` is now a misnomer — name asserts the exact false precondition (future already completed → `result(timeout)` vestigial) that caused BUG-06; the updated docstring says "may still be running". A maintainer trusting the name could simplify to `future.result()` or "restore" `as_completed`, silently reverting the fix. Only 3 reference sites (def, one call at `:488`, direct tests in `test_analysis_orchestrator.py:719-731`). Rename to e.g. `_wait_for_future_with_timeout`, or inline (see CR-13).
  - **Fixed 2026-07-25** (subsumed by CR-16): `_process_completed_future` deleted; the wait now runs through `utils/futures.py:map_with_per_item_timeout`.

- [x] **BUG-06-CR-13** (P3) Single-use 7-param helper + unnamed args tuple should be inlined — `_process_completed_future` has exactly one production caller; 5 of 7 params are pass-throughs to `_handle_enrichment_error`, `failed_candidates` is an out-param, and the call site packs an unnamed 7-tuple (`args = (...); self._process_completed_future(*args)`) that defeats signature help (operation_name/error_field both `str`, default_error_value `Any` — a reorder type-checks and runs silently wrong). Inlining the 10-line try/except into the loop deletes all of it; every name is already in scope. Direct unit test at `test_analysis_orchestrator.py:719-743` needs rework (end-to-end coverage preserved by the new regression tests).
  - **Fixed 2026-07-25** (subsumed by CR-16): helper and args-tuple deleted; direct test replaced by an end-to-end timed-out-enrichment test.

- [ ] **BUG-06-CR-14** (P3) Timeout smuggled as string via `error_message` — the split `except TimeoutError`/`except Exception` branches (`:438-447`) exist only to pass `error_message=str(timeout_seconds)`; `_handle_enrichment_error` re-dispatches with its own `isinstance` check and logs the string under `timeout_seconds=error_message` (`:405`, comment admits the smuggling). Simpler: one `except Exception` clause passing `timeout_seconds: Optional[int]`; the existing isinstance branch formats the message. Also fixes CR-09 (thread the resolved timeout into the sequential path or None-handle in the formatter).

- [x] **BUG-06-CR-15** (P4) `TimeoutError` import from `concurrent.futures` is redundant — verified `concurrent.futures.TimeoutError is TimeoutError` → True on 3.13 (alias since 3.11). `analysis_orchestrator.py:8` can shrink to `from concurrent.futures import ThreadPoolExecutor`; the import shadows the builtin with an identical object and falsely implies a futures-specific type distinction at `:401`/`:440`.
  - **Fixed 2026-07-25** (with CR-16): `concurrent.futures` imports removed from `analysis_orchestrator.py` entirely; builtin `TimeoutError` used.

- [x] **BUG-06-CR-16** (P2) Extract a shared per-future-timeout helper — the corrected loop duplicates the idiom at `multi_language_search.py:312-314` (`for future, lang in futures.items(): future.result(timeout=...)`), and that copy still runs inside `with ThreadPoolExecutor`, retaining the exact `shutdown(wait=True)` hang this fix's own comment documents (unbounded: `_execute_subprocess` in `core/executor.py:98-105` calls `subprocess.run` with no timeout). No futures helper exists in `core/` or `utils/`. Extract e.g. `core` `wait_futures_with_per_item_timeout(futures, timeout, on_error)` owning the corrected lifecycle; adopt in deduplication enrichment, cross_language search, then CR-01's batch coverage and CR-10's ranker.
  - **Fixed 2026-07-25:** extracted `utils/futures.py:map_with_per_item_timeout(items, func, timeout_seconds, max_workers, on_success, on_error)` owning the corrected lifecycle (per-item `result(timeout=)`, `shutdown(wait=False, cancel_futures=True)`, documented callback/abandonment contract). Adopted at all three sites: enrichment (both paths), batch coverage `_process_parallel_batch`, and `_run_parallel_search` — removing the cross_language `with ThreadPoolExecutor` latent hang (empirically proven: hung language search now returns promptly, logged via new `language_search_timed_out` event; regression tests in `TestRunParallelSearchTimeout`). Dead helpers deleted: `_process_completed_future`, `_get_future_result`, `_get_future_result_with_timeout`, `_submit_language_futures`, `_collect_future_results`. Helper unit tests: `tests/unit/test_futures.py`. **Adversarially verified** (3 lenses, no major issues in helper/sites; CancelledError unreachable, KeyboardInterrupt propagates promptly, duplicate items safe). Remaining adopter: CR-10's ranker.

- [ ] **BUG-06-CR-17** (P4) Fresh ThreadPoolExecutor per enrichment stage per call — `analysis_orchestrator.py:479` (predates the diff). Verified negligible (thread spawn µs–low-ms vs enrichment I/O), and per-call pools are the *defensible* choice under the abandonment strategy: a shared pool would let one hung candidate permanently steal a worker and starve later calls. Documented for completeness — no action unless the hang source is fixed first.

### Refactoring support (tests) — `tests/unit/test_orchestrator_optimizations.py`

- [ ] **BUG-06-CR-18** (P3) New timeout tests add ~4s of real sleeping to every unit run — measured 1.01s + 3.01s (`--durations`); the 3s inherits the N×T compounding (three sequential fresh 1s waits, including a full second on the never-started third candidate). Sub-second timeouts blocked by `timeout_per_candidate: Optional[int]` typing (`:544`); widen to float (futures accepts float timeouts) → both tests ~0.2s. The `assert elapsed < 15` bound also bakes N×T in as acceptable — an O(T) shared-deadline fix (CR-05) wouldn't register as an improvement.
- [ ] **BUG-06-CR-19** (P4) Two regression tests duplicate ~15-17 lines of scaffolding (orchestrator ctor, `release` Event, monotonic timing, full `_parallel_enrich` kwarg block, try/finally `release.set()`) — differ only in candidates, `default_error_value`, enrich_func, assertions. Extract class-local `_run(candidates, enrich_func, release, default_error_value) -> (failed, elapsed)`.
- [ ] **BUG-06-CR-20** (P4) `import threading`/`import time` duplicated inside both test bodies (`:869-870`, `:908-909`) — hoist to module level (three pre-existing local `import time` at `:110`, `:710`, `:736` could hoist too).
- [ ] **BUG-06-CR-21** (P4) Magic string `"Operation timed out after 1s"` duplicated (`:901`, `:936`) — hardcodes both the production format (`analysis_orchestrator.py:407`) and the timeout value 1 (`:893`/`:928`); tuning the timeout requires 4 synchronized edits. Violates CLAUDE.md "no magic numbers or strings". Fix: `TIMEOUT_SECONDS = 1`; `EXPECTED_TIMEOUT_ERROR = f"Operation timed out after {TIMEOUT_SECONDS}s"`.
- [ ] **BUG-06-CR-22** (P4) Unnamed timeout literals `60` (safety net, `:879`/`:916`) and `1` (`:893`/`:928`) repeated across both tests with no name expressing their required relationship (safety net ≫ candidate timeout). Name them (e.g. `HUNG_WORKER_SAFETY_NET_SECONDS`).
- [ ] **BUG-06-CR-23** (P4) Magic elapsed bounds `10` (`:899`) and `15` (`:934`) with implicit derivation (~timeout × candidate count + slack) — raising `timeout_per_candidate` 1→8 makes elapsed ~24s and spuriously fails `< 15` with no formula to recompute against. Derive the bound from the test parameters or name it with the derivation.

### Gap-sweep findings (second pass)

- [ ] **BUG-06-CR-24** (P2) Production flow cannot configure the now-live timeout — `timeout_per_candidate` appears nowhere in `config.py` or `tools.py`; `_enrich_and_summarize_with_config` calls `_add_recommendations` (`analysis_orchestrator.py:343`) and `_add_test_coverage_batch` (`:337`) passing only `parallel`/`max_workers`, so the 30s default (`constants.py:42`) is hardwired. Now that the timeout is enforced, any candidate whose enrichment legitimately needs >30s is spuriously marked `'Operation timed out after 30s'` on every analyze call with no MCP-level knob to raise the limit (reachable only from tests calling private methods). Fix: plumb `timeout_per_candidate` through `AnalysisConfig` and the `analyze_deduplication_candidates` tool signature.

- [x] **BUG-06-CR-25** (P2) `_add_test_coverage_batch` silently drops its `timeout_per_candidate` parameter — declared at `analysis_orchestrator.py:601`, never referenced in the body; the downstream call (`:622-624`) passes only `parallel`/`max_workers`, and `get_test_coverage_for_files_batch` (`coverage.py:472-479`) accepts no timeout parameter at all. A caller-supplied timeout is discarded with no warning while the API implies protection exists. Standalone fix vehicle for CR-01: thread the timeout through the batch coverage path. Contrast: `_add_recommendations` (`:670`) forwards it correctly.
  - **Fixed 2026-07-24** (with CR-01): `_add_test_coverage_batch` now passes `timeout_per_file=timeout_per_candidate` to `get_test_coverage_for_files_batch`, which accepts and threads it into `_process_parallel_batch`. Note: CR-24 (no MCP/config-level knob for the timeout) still open — the parameter is live but only reachable internally.

- [ ] **BUG-06-CR-26** (P3) `ParallelProcessing.MAX_TIMEOUT_SECONDS=300` ("5 minutes max total timeout", `constants.py:43`) is dead config — grep shows exactly three references: the definition and two test assertions that it exists (`test_orchestrator_optimizations.py:769/771`). The new per-future wait loop is precisely the aggregate-wait site the comment describes yet never consults it (see CR-05: actual worst case ~3000s, 10× the documented cap). Fix: enforce it as the shared deadline in CR-05's fix, or delete the constant and its comment.

- [ ] **BUG-06-CR-27** (P3) Per-candidate timeout is position-dependent; a slow candidate can pass — `future.result(timeout=)` (`analysis_orchestrator.py:439`) starts its clock when the loop *reaches* each future, not when the candidate started executing. Verified: `max_workers=2`, T=30s, [A(hung), B(45s)] both start at t=0 — A times out at t=30, then `B.result(30)` is called at t=30 and B (finished t=45, ran 45s > 30s) returns as success; swap dict order and B is marked timed out. Same workload, opposite outcome; docstrings (`:557`, `:678`) promise a per-candidate cap the loop doesn't deliver. Permissive error (completed work kept), liveness unaffected. Fix: document the wait-based semantics, or compute a shared deadline from loop start (CR-05).

- [ ] **BUG-06-CR-28** (P4, PLAUSIBLE) Regression test enshrines the never-started mislabeling — `test_all_hung_candidates_each_marked_timed_out` (`:936`) asserts the never-started third candidate (both workers hung; c2 never dequeued) carries the identical `'Operation timed out after 1s'` error. Fixing CR-07 (distinct 'cancelled before start' label or early `future.cancel()`) breaks this assertion, forcing rework of a test that appears to guard BUG-06. Fix alongside CR-07: assert bounded return + failure marking without pinning the exact label for never-started candidates.

- [ ] **BUG-06-CR-29** (P4, PLAUSIBLE) 1s per-candidate timeout risks flake on saturated CI — `test_hung_enrichment_times_out_and_marks_candidate` (`:903`) gives the fast worker an effective ~2s wall-clock window (hung's 1s wait + fast's own 1s result window) to complete a one-line assignment; OS-level starvation for ~2s (severely oversubscribed runner, paused VM) fails `failed == [hung]`/`fast["done"]`/`"error" not in fast` intermittently. No pytest-rerunfailures/flaky guard in pyproject.toml. Both worker threads start eagerly with the GIL released during the window, so exposure is rare and environment-dependent. Mitigation: widen the fast candidate's margin (larger `timeout_per_candidate` for the fast-path test, or float timeouts per CR-18).

## Low-Priority Findings (dedup module review, 2026-07-21)

- [x] **BUGL-01** (P3) Suggestion line savings assume all group instances have `group[0]`'s length — `detector.py:627-634` computes `lines * len(group)`; groups hold *similar* code of differing lengths, so `total_duplicated_lines`/`potential_line_savings` are wrong whenever lengths differ. Nearby `_meets_min_savings` (`detector.py:536-537`) counts per-item correctly — a group can pass the filter yet report a different savings number.
- [ ] **BUGL-03** (P3) Diff summary miscounts multi-line replacements — `diff.py:42-53` pairs a `-` line with a `+` only when directly adjacent, but unified diffs emit all `-` then all `+` per hunk, so a 2-line replacement counts as deletion+modification+addition and `old`/`new` lines are mis-paired in the `changes` list.
- [ ] **BUGL-08** (P4) Impact analyzer path/location parsing latent bugs — `impact.py:259-263` filters exclusions on raw relative paths before absolutizing (`:267-269`), so absolute `exclude_files` never match and duplicates' own files inflate `breaking_change_risk`; `impact.py:165-171` `_parse_files_from_locations` only parses `"file:line"` strings while detector suggestions emit dicts (`detector.py:623`), so `files_in_group` is always empty. No production caller of `analyze_deduplication_impact` found in `src/` — latent at the module boundary.
- [x] **BUGL-09** (P4) `_get_nesting_depth`-style check returns max depth *before* the identifier, not depth *at* it — `analyzer.py:803-819` returns `max_depth` instead of the running `depth`, so `f(g(x)) + identifier` reports the identifier as nested (depth 2) when it sits at depth 0, and `_find_nested_call_in_matches` falsely flags it.

## Deferred

- [ ] **DRY-01** (P1) Replace `_run_tool()` with `tool_context` in cross_language — consolidate timing + error handling in `cross_language/tools.py:40-57` with shared `tool_context`. Removes ~20 lines and aligns with all other tool modules. Affects 5 cross-language tools. -- `src/ast_grep_mcp/features/cross_language/tools.py:40-57` (2026-03-08 DRY review)
- [ ] **DRY-02** (P1) Replace hardcoded exclude patterns with `FilePatterns` — 8 files (detector.py, sync_checker.py, readme_generator.py, api_docs_generator.py, security_scanner.py, estimator.py, executor.py, orphan.py) hardcode their own exclude lists instead of using `constants.FilePatterns.DEFAULT_EXCLUDE` and `SKIP_DIR_NAMES`. Removes ~40 lines and establishes single source of truth. -- `src/ast_grep_mcp/constants.py` + 8 files (2026-03-08 DRY review)
- [ ] **DRY-03** (P2) Adopt `read_file_lines` + `write_file_lines` in 4 files — polyglot_refactoring.py, renamer.py, fixer.py, sync_checker.py each manually open/read files. Consolidate via shared utils. Removes ~12 lines and ensures consistent error handling. -- `src/ast_grep_mcp/utils/text.py` (2026-03-08 DRY review)
- [ ] **DRY-04** (P3) Remove redundant re-timing inside `tool_context` — 20+ tools in condense, documentation, schema, quality modules calculate elapsed time manually after `tool_context` already reports it. Removes ~30 lines of noise. -- `src/ast_grep_mcp/features/{condense,documentation,schema,quality}/tools.py` (2026-03-08 DRY review)
- [ ] **DRY-06** (P3) Consolidate `LANGUAGE_EXTENSIONS` maps — `analyze_codebase.py:39` defines `{lang: "ext"}` (singular) while `src/ast_grep_mcp/constants.py:361` defines `{lang: ["ext", ...]}` (list). `scripts/api_docs_generator.py` has a third variant. Converge on one shape in `constants.py` and adopt across scripts. -- `analyze_codebase.py:39`, `scripts/api_docs_generator.py` (2026-04-23 analyze_codebase reuse review)
- [ ] **DRY-07** (P3) Extract shared `find_source_files(root, language, exclude_patterns)` helper — `analyze_codebase.py:74`, `scripts/sync_checker.py`, and `scripts/api_docs_generator.py` each reinvent `rglob + exclude` discovery. Candidate home: `utils/file_discovery.py`. -- `src/ast_grep_mcp/utils/` (2026-04-23 analyze_codebase reuse review)
- [ ] **DRY-08** (P3) Extract `_run_tsc_check`-style subprocess runner — `analyze_codebase.py:335-371` duplicates subprocess-with-timeout + output-parsing patterns found in `features/rewrite/service.py` and formatter wrappers. Candidate home: `core/executor.py` or new `utils/subprocess_runner.py`. -- `analyze_codebase.py:335` (2026-04-23 analyze_codebase reuse review)
- [ ] **DRY-09** (P4) Delete `out()` wrapper in `analyze_codebase.py:53-55` — thin passthrough to `console.log(str(...))`; ~40 callsites could call `console.log` directly. Low priority (churn vs. clarity). -- `analyze_codebase.py:53` (2026-04-23 analyze_codebase reuse review)
- [ ] **HC-01** (P2) Add ESLint to pre-commit hooks — integrate `npm run lint:fix && npm run check` for TypeScript verification at commit time. Reduces lint-on-CI friction and catches issues earlier. -- `hooks/`
- [ ] **DF-01** (Low) Strategy pattern filter for deduplication — per `docs/duplicate-detector-misses.md` investigation. Only candidate (Group 5) would save ~18 lines with minor signature mismatch; over-engineering for marginal benefit. (deferred 2026-03-08)
- [ ] **CF-04** (P3) Config-aware search mode — complex feature for PM2/Zod/JSON-LD config patterns. Deferred from 2026-03-11 session as out of scope. -- `src/ast_grep_mcp/features/search/`
- [ ] **SR-01** (P2) Unit tests for security scanner — add pytest test file for `detect_security_issues_impl()` covering SQL injection, XSS, command injection, secrets, and crypto patterns. Currently no test coverage. -- `tests/unit/test_detect_security_issues.py` (new)
- [ ] **SR-02** (P3) Integration tests for Python security YAML rules — validate that 12 rules in `rules/python-security-high-priority.yaml` fire correctly on sample vulnerable code. Add test fixtures for each CWE. -- `tests/integration/test_python_security_rules.py` (new)
- [ ] **SR-03** (P3) Document Python security rule coverage — create guide explaining which CWEs are covered (798, 327, 489, 521, 532, 377, 295), known limitations (e.g., follow/precedes matching edge cases), and migration path for integrating rules into quality/rules.py template system. -- `docs/PYTHON-SECURITY-RULES-GUIDE.md` (new)
- [ ] **SR-04** (P4) Expand security pattern examples — add coverage for additional CWE types (330: Use of Insufficiently Random Values, 434: Unrestricted Upload, 611: XXE). Currently 30+ patterns focused on CWE-798/327/489/521. -- `src/ast_grep_mcp/features/search/pattern_examples.json`

## Completed Items (Migrated to Changelog)

- [x] **LM-01–LM-05** Library Migration Phase 1–2 → [docs/changelog/2026-04-19-library-migration-phase1-phase2.md](changelog/2026-04-19-library-migration-phase1-phase2.md)
- [x] **FG-01** Schema detection Liquid/Jekyll template fallback → [docs/changelog/2026-04-20-schema-liquid-jekyll-fallback.md](changelog/2026-04-20-schema-liquid-jekyll-fallback.md)
- [x] **MQ-01–MQ-02** Minor Code Quality Items → [docs/changelog/2026-03-12-minor-code-quality-items.md](changelog/2026-03-12-minor-code-quality-items.md)
- [x] **PA-01–PA-05** Pattern Analysis performance optimization → [docs/changelog/2026-04-20-pattern-analysis-performance-optimization.md](changelog/2026-04-20-pattern-analysis-performance-optimization.md)
- [x] **PA-06** Parallel candidate scoring → [docs/changelog/2026-04-20-pa06-parallel-scoring.md](changelog/2026-04-20-pa06-parallel-scoring.md)
- [x] **BUG-01, BUG-03–BUG-05, BUGL-02, BUGL-04–BUGL-07, BUG-14, DRY-05, FLAKY-01** Deduplication quality fixes → [docs/changelog/2026-07-21-deduplication-quality-fixes.md](changelog/2026-07-21-deduplication-quality-fixes.md)

