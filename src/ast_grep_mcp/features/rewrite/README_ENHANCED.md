# rewrite

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "SoftwareSourceCode",
  "name": "rewrite",
  "description": "Directory containing 3 code files with 0 classes and 23 functions",
  "programmingLanguage": [
    {
      "@type": "ComputerLanguage",
      "name": "Python"
    }
  ],
  "featureList": [
    "23 function definitions"
  ]
}
</script>

## Overview

This directory contains 3 code file(s) with extracted schemas.

## Files and Schemas

### `backup.py` (python)

**Functions:**
- `create_backup(files_to_backup, project_folder) -> str` - Line 13
- `create_deduplication_backup(files_to_backup, project_folder, duplicate_group_id, strategy, original_hashes) -> str` - Line 68
- `get_file_hash(file_path) -> str` - Line 144
- `verify_backup_integrity(backup_id, project_folder) -> Dict[...]` - Line 160
- `restore_backup(backup_id, project_folder) -> Dict[...]` - Line 227
- `_calculate_backup_size(backup_dir) -> int` - Line 286
- `_build_backup_info(metadata, backup_name, backup_size) -> Dict[...]` - Line 298
- `_load_backup_info(backup_dir, backup_name, logger) -> Optional[...]` - Line 329
- `list_available_backups(project_folder) -> List[...]` - Line 358

**Key Imports:** `ast_grep_mcp.core.logging`, `datetime`, `hashlib`, `json`, `os` (+2 more)

### `service.py` (python)

**Functions:**
- `_validate_python_syntax(content, file_path) -> Dict[...]` - Line 27
- `_validate_javascript_syntax(content) -> Dict[...]` - Line 44
- `_validate_java_syntax(file_path) -> Dict[...]` - Line 71
- `validate_syntax(file_path, language) -> Dict[...]` - Line 94
- `validate_rewrites(modified_files, language) -> Dict[...]` - Line 137
- `_validate_yaml_rule(yaml_rule) -> Dict[...]` - Line 172
- `_create_temp_rule_file(yaml_rule) -> str` - Line 202
- `_build_command_args(rule_file, project_folder, max_file_size_mb, workers, language) -> Tuple[...]` - Line 216
- `_perform_dry_run(args, search_targets, logger, start_time) -> Dict[...]` - Line 250
- `_apply_rewrites(args, search_targets, rule_file, project_folder, backup, workers, language, logger, start_time) -> Dict[...]` - Line 301
- ... and 3 more functions

**Key Imports:** `ast_grep_mcp.core.exceptions`, `ast_grep_mcp.core.executor`, `ast_grep_mcp.core.logging`, `ast_grep_mcp.features.rewrite.backup`, `json` (+7 more)

### `tools.py` (python)

**Functions:**
- `register_rewrite_tools(mcp) -> Constant(value=None, kind=None)` - Line 15

**Key Imports:** `ast_grep_mcp.features.rewrite.service`, `mcp.server.fastmcp`, `pydantic`, `typing`

---
*Generated by Enhanced Schema Generator with schema.org markup*