# refactoring

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "SoftwareSourceCode",
  "name": "refactoring",
  "description": "Directory containing 5 code files with 4 classes and 5 functions",
  "programmingLanguage": [
    {
      "@type": "ComputerLanguage",
      "name": "Python"
    }
  ],
  "featureList": [
    "4 class definitions",
    "5 function definitions"
  ]
}
</script>

## Overview

This directory contains 5 code file(s) with extracted schemas.

## Files and Schemas

### `analyzer.py` (python)

**Classes:**
- `CodeSelectionAnalyzer` - Line 24
  - Analyzes code selections for refactoring operations.
  - Methods: __init__, analyze_selection, _detect_indentation, _analyze_variables, _find_python_assignments (+14 more)

**Key Imports:** `ast_grep_mcp.core.logging`, `models.refactoring`, `re`, `typing`

### `extractor.py` (python)

**Classes:**
- `FunctionExtractor` - Line 24
  - Handles function extraction refactoring.
  - Methods: __init__, extract_function, _generate_function_name, _generate_signature, _generate_docstring (+11 more)

**Key Imports:** `ast_grep_mcp.core.logging`, `features.rewrite.backup`, `models.refactoring`, `os`, `typing`

### `rename_coordinator.py` (python)

**Classes:**
- `RenameCoordinator` - Line 25
  - Coordinates multi-file symbol renaming.
  - Methods: __init__, rename_symbol, _generate_diff_preview, _apply_rename, _rename_in_file

**Key Imports:** `ast_grep_mcp.core.logging`, `features.rewrite.backup`, `models.refactoring`, `re`, `renamer` (+1 more)

### `renamer.py` (python)

**Classes:**
- `SymbolRenamer` - Line 26
  - Handles symbol renaming operations.
  - Methods: __init__, find_symbol_references, _get_symbol_pattern, _matches_filter, _classify_reference (+17 more)

**Key Imports:** `ast_grep_mcp.core.logging`, `core.executor`, `fnmatch`, `json`, `models.refactoring` (+3 more)

### `tools.py` (python)

**Functions:**
- `_format_extract_function_response(result, selection, language) -> Dict[...]` - Line 17
- `extract_function_tool(project_folder, file_path, start_line, end_line, language, function_name, extract_location, dry_run) -> Dict[...]` - Line 56
- `extract_function(project_folder, file_path, start_line, end_line, language, function_name, extract_location, dry_run) -> Dict[...]` - Line 170
- `rename_symbol_tool(project_folder, symbol_name, new_name, language, scope, file_filter, dry_run) -> Dict[...]` - Line 196
- `rename_symbol(project_folder, symbol_name, new_name, language, scope, file_filter, dry_run) -> Dict[...]` - Line 312

**Key Imports:** `analyzer`, `ast_grep_mcp.core.logging`, `extractor`, `models.refactoring`, `os` (+3 more)

---
*Generated by Enhanced Schema Generator with schema.org markup*